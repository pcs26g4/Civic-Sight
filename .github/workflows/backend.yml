name: Deploy Backend
on:
  push:
    branches:
      - main
    paths:
      - 'Backend/**'
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy to Render
        run: |
          curl -X POST "https://api.render.com/deploy/srv-d622ehkhg0os7385im1g?key=witBZCeOmzk"
